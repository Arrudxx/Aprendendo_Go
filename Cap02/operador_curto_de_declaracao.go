package main

import "fmt"

func main() {

	//Uso:
	// Operador de Declaração: := (e só pode ser usando dentro de um codeblock)
	// Operador de Atribuição: = )(para atribuir valores a variáveis já existentes)

	// x := 10
	// y := "Olá bom dia"
	// fmt.Printf("x: %v, %T\n", x, x)
	// fmt.Printf("x: %v, %T\n\n", y, y)

	// x, z := 20, 30
	// fmt.Printf("x: %v, %T\n", x, x)
	// fmt.Printf("z: %v, %T\n", z, z)

	// Terminologia:

	x := 10 < 20

	fmt.Println(x)

}
